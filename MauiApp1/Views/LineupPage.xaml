<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.LineupPage"
             Title="Начальные расстановки">

    <ScrollView>

        <Grid RowDefinitions="auto,*" ColumnDefinitions="35*,15*,35*">

            <Label x:Name="NameTeamHome" Grid.Row="0" Grid.Column="0" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" TextColor="White"/>

            <Label x:Name="NameTeamGuest" Grid.Row="0" Grid.Column="2" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" TextColor="White"/>

            <Grid RowDefinitions="*,*,*" ColumnDefinitions="*,*" Grid.Row="1" Grid.Column="0">

                <Border Grid.Row="0" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel5"
                                    Text="Зона 5"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker5"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel4"
                                    Text="Зона 4"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker4"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel6"
                                    Text="Зона 6"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker6"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel3"
                                    Text="Зона 3"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker3"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel1"
                                    Text="Зона 1"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker1"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="#007ACC">

                    <Grid>

                        <Label x:Name="homePosLabel2"
                                    Text="Зона 2"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="homePosPicker2"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DodgerBlue"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

            </Grid>

            <Grid RowDefinitions="*,*,*" ColumnDefinitions="*,*" Grid.Row="1" Grid.Column="2">

                <Border Grid.Row="0" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel2"
                                    Text="Зона 2"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker2"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel1"
                                    Text="Зона 1"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker1"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel3"
                                    Text="Зона 3"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker3"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel6"
                                    Text="Зона 6"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker6"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="0" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel4"
                                    Text="Зона 4"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker4"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="1" Margin="5" StrokeShape="RoundRectangle 15" BackgroundColor="Chocolate">

                    <Grid>

                        <Label x:Name="guestPosLabel5"
                                    Text="Зона 5"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="Title">
                        </Label>

                        <Picker x:Name="guestPosPicker5"
                                    Title="Выберите игрока"
                                    Opacity="0"                                    
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters/>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="SandyBrown"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

            </Grid>



            <Grid Grid.Row="1" Grid.Column="1" RowDefinitions="*,*,*">

                <Button
                Grid.Row="0"
                BackgroundColor="MediumPurple"
                TextColor="White"
                Text="Старт" 
                FontSize="Title"  
                HorizontalOptions="Fill" 
                VerticalOptions="Fill"
                Clicked="OnStartMatchClicked"
                Margin="30,10,30,10">

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="MediumOrchid"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                </Button>

                <Button 
                    Grid.Row="1" 
                    Text="⇄" 
                    BackgroundColor="MediumPurple"
                    TextColor="White"
                    FontSize="30" 
                    FontAttributes="Bold"
                    HorizontalOptions="Fill" 
                    VerticalOptions="Fill"
                    Clicked="OnReverseClicked"
                    Margin="30,20,30,20">

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="MediumOrchid"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                </Button>

            </Grid>
            
        </Grid>    

    </ScrollView>

</ContentPage>