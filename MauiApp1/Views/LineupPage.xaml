<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.LineupPage"
             Title="Начальная расстановка"             
             BackgroundColor="#FAFAFA">

    <ScrollView>

        <Grid RowDefinitions="10*,80*,10*" ColumnDefinitions="40*,10*,40*">

            <Label x:Name="NameTeamHome" Grid.Row="0" Grid.Column="0" Text="Экономист" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" FontFamily="Arial"/>

            <Label x:Name="NameTeamGuest" Grid.Row="0" Grid.Column="2" Text="Сбербанк" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" FontFamily="Arial"/>

            <Grid RowDefinitions="*,*,*" ColumnDefinitions="*,*" Grid.Row="1" Grid.Column="0">

                <Border Grid.Row="0" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel5"
                                    Text="Зона 5"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker5"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel4"
                                    Text="Зона 4"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker4"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel6"
                                    Text="Зона 6"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker6"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel3"
                                    Text="Зона 3"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker3"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel1"
                                    Text="Зона 1"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker1"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="homePosLabel2"
                                    Text="Зона 2"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="homePosPicker2"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedHome">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

            </Grid>

            <Grid RowDefinitions="*,*,*" ColumnDefinitions="*,*" Grid.Row="1" Grid.Column="2">

                <Border Grid.Row="0" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel2"
                                    Text="Зона 2"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker2"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel1"
                                    Text="Зона 1"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker1"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel3"
                                    Text="Зона 3"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker3"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="1" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel6"
                                    Text="Зона 6"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker6"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="0" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel4"
                                    Text="Зона 4"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker4"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

                <Border Grid.Row="2" Grid.Column="1" Margin="10" StrokeShape="RoundRectangle 25">

                    <Grid>

                        <Label x:Name="guestPosLabel5"
                                    Text="Зона 5"
                                    TextColor="White"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"
                                    FontSize="26">
                        </Label>

                        <Picker x:Name="guestPosPicker5"
                                    Title="Выберите игрока"
                                    Opacity="0"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill"
                                    SelectedIndexChanged="OnPlayerSelectedGuest">
                        </Picker>

                    </Grid>

                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <!--Нормальное состояние-->
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray"/>
                                    </VisualState.Setters>
                                </VisualState>

                                <!--Наведение мыши (работает на Desktop)-->
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Gray"/>
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>

                </Border>

            </Grid>

            <Button 
                Grid.Row="2" 
                Grid.Column="0" 
                Grid.ColumnSpan="3" 
                Text="Начать партию" 
                FontSize="24" 
                FontFamily="Arial" 
                HorizontalOptions="Fill" 
                VerticalOptions="End" 
                Margin="20,0,20,10"
                Clicked="OnStartMatchClicked"/>

            <Button 
                Grid.Row="0" 
                Grid.Column="1" 
                Text="Смена" 
                FontSize="24" 
                FontFamily="Arial" 
                HorizontalOptions="Center" 
                VerticalOptions="Center"
                Clicked="OnReverseClicked"/>

        </Grid>    

    </ScrollView>

</ContentPage>