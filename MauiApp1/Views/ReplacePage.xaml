
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.ReplacePage"
             Title="ReplacePage">

    <Grid ColumnDefinitions="*,*,*">

        <Frame Grid.Column="0" BorderColor="{DynamicResource MainColor}" Background="Transparent" CornerRadius="10" Padding="10" Margin="15">

            <VerticalStackLayout>

                <Label Text="Игроки на поле" FontSize="Title" Margin="0,0,0,10" HorizontalOptions="Center" VerticalOptions="Start" FontAttributes="Bold" TextColor="White"/>

                <CollectionView x:Name="ListPlayerIn" SelectionMode="Single">

                    <CollectionView.ItemsLayout>

                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>

                    </CollectionView.ItemsLayout>                    

                    <CollectionView.ItemTemplate>

                        <DataTemplate>

                            <Border Stroke="{DynamicResource MainColor}" Padding="10">

                                <VisualStateManager.VisualStateGroups>

                                    <VisualStateGroupList>

                                        <VisualStateGroup x:Name="CommonStates">

                                            <VisualState x:Name="Normal">

                                                <VisualState.Setters>

                                                    <Setter Property="Background" Value="#141414"/>

                                                </VisualState.Setters>

                                            </VisualState>

                                            <VisualState x:Name="Selected">

                                                <VisualState.Setters>

                                                    <Setter Property="Background" Value="{DynamicResource ReplaceSelectColor}"/>

                                                </VisualState.Setters>

                                            </VisualState>

                                        </VisualStateGroup>

                                    </VisualStateGroupList>

                                </VisualStateManager.VisualStateGroups>

                                <Border.StrokeShape>

                                    <RoundRectangle CornerRadius="10"/>

                                </Border.StrokeShape>

                                <Grid ColumnDefinitions="auto,*">

                                    <Label Grid.Column="0" Text="{Binding Number}" Margin="0,0,10,0"/>
                                    <Label Grid.Column="1" Text="{Binding Name}" Margin="10,0,0,0"/>

                                </Grid>

                            </Border>

                        </DataTemplate>

                    </CollectionView.ItemTemplate>

                </CollectionView>

            </VerticalStackLayout>                     

        </Frame>

        <Frame Grid.Column="1" BorderColor="{DynamicResource ExitColor}" Background="Transparent" CornerRadius="10" Padding="10" Margin="15">

            <VerticalStackLayout>

                <Label Text="Игроки на замене" FontSize="Title" Margin="0,0,0,10" HorizontalOptions="Center" VerticalOptions="Start" FontAttributes="Bold" TextColor="White"/>

                <CollectionView x:Name="ListPlayerOut" SelectionMode="Single">

                    <CollectionView.Resources>

                        <Style TargetType="Border">

                            <Setter Property="VisualStateManager.VisualStateGroups">

                                <VisualStateGroupList>

                                    <VisualStateGroup x:Name="CommonStates">

                                        <VisualState x:Name="Normal">

                                            <VisualState.Setters>

                                                <Setter Property="Background" Value="Transparent" />

                                            </VisualState.Setters>

                                        </VisualState>

                                        <VisualState x:Name="Selected">

                                            <VisualState.Setters>

                                                <Setter Property="Background" Value="Transparent" />

                                            </VisualState.Setters>

                                        </VisualState>

                                    </VisualStateGroup>

                                </VisualStateGroupList>

                            </Setter>

                        </Style>

                    </CollectionView.Resources>

                    <CollectionView.ItemsLayout>

                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>

                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>

                        <DataTemplate>

                            <Border Stroke="{DynamicResource ExitColor}" Padding="10">

                                <VisualStateManager.VisualStateGroups>

                                    <VisualStateGroupList>

                                        <VisualStateGroup x:Name="CommonStates">

                                            <VisualState x:Name="Normal">

                                                <VisualState.Setters>

                                                    <Setter Property="Background" Value="#141414"/>

                                                </VisualState.Setters>

                                            </VisualState>

                                            <VisualState x:Name="Selected">

                                                <VisualState.Setters>

                                                    <Setter Property="Background" Value="{DynamicResource PointExitColor}"/>

                                                </VisualState.Setters>

                                            </VisualState>

                                        </VisualStateGroup>

                                    </VisualStateGroupList>

                                </VisualStateManager.VisualStateGroups>

                                <Border.StrokeShape>

                                    <RoundRectangle CornerRadius="10"/>

                                </Border.StrokeShape>

                                <Grid ColumnDefinitions="auto,*">

                                    <Label Grid.Column="0" Text="{Binding Number}" Margin="0,0,10,0"/>
                                    <Label Grid.Column="1" Text="{Binding Name}" Margin="10,0,0,0"/>

                                </Grid>

                            </Border>

                        </DataTemplate>

                    </CollectionView.ItemTemplate>

                </CollectionView>

            </VerticalStackLayout>

        </Frame>

        <VerticalStackLayout Grid.Column="2" VerticalOptions="Start">

            <Button x:Name="ButtonReplace" Clicked="OnReplaceButtonClick" Text="Замена" TextColor="White" VerticalOptions="Start" Margin="20,70,20,20">

                <VisualStateManager.VisualStateGroups>

                    <VisualStateGroupList>

                        <VisualStateGroup x:Name="CommonStates">

                            <VisualState x:Name="Normal">

                                <VisualState.Setters>

                                    <Setter Property="Background" Value="{DynamicResource MainColor}"/>

                                </VisualState.Setters>

                            </VisualState>

                            <VisualState x:Name="PointerOver">

                                <VisualState.Setters>

                                    <Setter Property="Background" Value="{DynamicResource PointColor}"/>

                                </VisualState.Setters>

                            </VisualState>

                        </VisualStateGroup>

                    </VisualStateGroupList>

                </VisualStateManager.VisualStateGroups>
                
            </Button>

            <Button x:Name="ButtonExit" Clicked="OnExitButtonClick" Text="Отмена" TextColor="White" VerticalOptions="Center" Margin="20,10,20,20">

                <VisualStateManager.VisualStateGroups>

                    <VisualStateGroupList>

                        <VisualStateGroup x:Name="CommonStates">

                            <VisualState x:Name="Normal">

                                <VisualState.Setters>

                                    <Setter Property="Background" Value="{DynamicResource ExitColor}"/>

                                </VisualState.Setters>

                            </VisualState>

                            <VisualState x:Name="PointerOver">

                                <VisualState.Setters>

                                    <Setter Property="Background" Value="{DynamicResource PointExitColor}"/>

                                </VisualState.Setters>

                            </VisualState>

                        </VisualStateGroup>

                    </VisualStateGroupList>

                </VisualStateManager.VisualStateGroups>

            </Button>
        
        </VerticalStackLayout>

    </Grid>
    
</ContentPage>